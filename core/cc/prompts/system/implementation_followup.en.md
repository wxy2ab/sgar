Continue the code implementation. Start writing code now.
Follow the system rules strictly:
1. Start from the first incomplete `- [ ]` task in tasks.md and use `file_write` to create or modify the corresponding code file
2. After writing the code, immediately use `plan_artifact_write` to mark that task as `[x]`
3. Do not repeatedly read files you have already read; write code directly
Do not output summary text. Start calling tools immediately.
