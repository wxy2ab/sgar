You just completed code changes, but `tasks.md` has not been updated yet.
Do not continue exploring, do not read unrelated files, and do not start another implementation step yet.
Follow this order strictly:
1. Immediately use `plan_artifact_write(artifact="tasks", merge_mode="replace")` to update `tasks.md`
2. Mark the task(s) completed in this round as `[x]`
3. Leave all remaining incomplete tasks unchanged
4. After updating `tasks.md`, continue with the next incomplete task
Do not output a summary. Start calling tools immediately.
