你刚刚已经完成了代码修改，但 `tasks.md` 还没有同步更新。
现在不要继续探索、不要读取无关文件、不要再次开始新的实现步骤。
请严格按以下顺序执行：
1. 立即使用 `plan_artifact_write(artifact="tasks", merge_mode="replace")` 更新 `tasks.md`
2. 将本轮已经完成的任务标记为 `[x]`
3. 保持其余未完成任务不变
4. 更新完成后，再继续下一个未完成任务
不要输出总结文字，直接开始调用工具。
